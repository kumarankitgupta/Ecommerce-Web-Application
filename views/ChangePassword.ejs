<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <title>Document</title>
</head>
<body>
    <h1>Password Validation</h1>
	<p>Please enter a password that meets the following requirements:</p>
	<ul class="error">
		<li>At least 8 characters long</li>
		<li>Contains at least one uppercase letter</li>
		<li>Contains at least one lowercase letter</li>
		<li>Contains at least one number</li>
	</ul>
    <form method="post" action="/users/changepassword" style="width: 40%;" class="m-lg-4">
        <h2>Change Password</h2>
        <div class="input-group">
        <input class="form-control" type="password" id="password" name="pass" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters" style="border-right:none ;" required> 
        <i class="bi bi-eye-slash  input-group-text" id="togglePassword1" onclick="togglePassword('password')" style="background-color:white; border-left:none"></i>
        </div>
        <label class="form-label" for="form3Example4c">Password</label>
        <div class="input-group">
            <input class="form-control" type="password" id="confirmPassword" name="cpass" onkeyup="verify()" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters" style="border-right:none ;" required> 
            <i class="bi bi-eye-slash  input-group-text" id="togglePassword2" onclick="togglePassword('confirmPassword')" style="background-color:white; border-left:none"></i>
            </div>
            <label class="form-label" for="form3Example4c" >Confirm Password</label>
        <div id="passwordMessage" style="color: red;"></div>
		<input type="submit" value="Change" id="submit" style="display: none;">
	</form>
    <script>
        
        function verify(){
        var pass1 = document.getElementById("password").value;
        var pass2 = document.getElementById("confirmPassword").value;
        var msg = document.getElementById("passwordMessage");
        console.log(pass1)
        console.log(pass2)
            if(pass1 != pass2){
            msg.innerHTML = "Password not matched"
            document.getElementById("submit").style.display='none';
        }else{
            msg.innerHTML = "";
            document.getElementById("submit").style.display='block';
        }
        }
        function togglePassword(id) {
			var x = document.getElementById(id);
			if (x.type === "password") {
				x.type = "text";
                var xi = document.getElementById(id)
                xi.setAttribute('class','bi bi-eye  input-group-text')
			} else {
				x.type = "password";
                var xi = document.getElementById(id)
                xi.setAttribute('class','bi bi-eye-slash  input-group-text')
			}
		}
    </script>
</body>
</html>